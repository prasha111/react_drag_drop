import React, { CSSProperties, ReactNode } from 'react';
export interface SliceProps {
    width: number;
    height: number;
    size?: number;
    children?: ReactNode;
    className?: string;
    style?: CSSProperties;
}
export declare function Slice({ width, height, size, children, className, style }: SliceProps): React.JSX.Element;
