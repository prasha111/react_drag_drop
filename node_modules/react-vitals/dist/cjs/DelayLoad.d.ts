import React, { ReactNode } from 'react';
export interface DelayLoadProps {
    /**
     * Milliseconds to wait before rendering children
     * @default 3000
     */
    timeout?: number;
    children?: ReactNode;
}
export declare function DelayLoad({ timeout, children }: DelayLoadProps): React.JSX.Element;
