import React from 'react';
export function Slice({ width, height, size = 50, children, className, style }) {
    const column = Math.ceil(width / size);
    const row = Math.ceil(height / size);
    const cells = [];
    for (let i = 0; i < row; i++) {
        for (let j = 0; j < column; j++) {
            cells.push(React.createElement("div", { key: i + '-' + j, style: {
                    position: 'absolute',
                    width: size,
                    height: size,
                    overflow: 'hidden',
                    left: j * size,
                    top: i * size,
                } },
                React.createElement("div", { style: { position: 'absolute', left: -j * size, top: -i * size } }, children)));
        }
    }
    return (React.createElement("div", { className: className, style: Object.assign(Object.assign({ position: 'relative', overflow: 'hidden' }, style), { width, height }) }, cells));
}
